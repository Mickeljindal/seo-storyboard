# Social posts: Deploying a Rails App: The Questions Everyone Actually Asks

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/deploy-rails-app/

## X / Twitter
```
A Rails deploy is an ordered sequence: `bundle install`, `assets:precompile`, `db:migrate`, then boot **Puma**.

https://www.kloudbean.com/blog/deploy-rails-app/
#Kloudbean #CloudHosting #DevOps #WebDev #Deployment
```

## LinkedIn
```
A Rails deploy is an ordered sequence: `bundle install`, `assets:precompile`, `db:migrate`, then boot **Puma**.

Set `RAILS_ENV=production`, `RAILS_MASTER_KEY` (or `SECRET_KEY_BASE`) and `DATABASE_URL` as env vars first, or it won't start. Uploads go to object storage via Active Storage. Add Sidekiq only if you have background jobs.

Read the full guide: https://www.kloudbean.com/blog/deploy-rails-app/

#Kloudbean #CloudHosting #DevOps #WebDev #Deployment
```

## X thread
```
1/6  Deploying a Rails App: The Questions Everyone Actually Asks

Quick thread 🧵
```
```
2/6  A Rails deploy is an ordered sequence: `bundle install`, `assets:precompile`, `db:migrate`, then boot **Puma**.
```
```
3/6  Set `RAILS_ENV=production`, `RAILS_MASTER_KEY` (or `SECRET_KEY_BASE`) and `DATABASE_URL` as env vars first, or it won't start.
```
```
4/6  Uploads go to object storage via Active Storage.
```
```
5/6  Add Sidekiq only if you have background jobs.
```
```
6/6  Full walkthrough:
https://www.kloudbean.com/blog/deploy-rails-app/
#Kloudbean #CloudHosting #DevOps #WebDev #Deployment
```

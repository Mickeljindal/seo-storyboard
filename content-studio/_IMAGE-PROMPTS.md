# Image prompts for the remaining article slots

Auto-generated by `scripts/write-image-prompts.mjs`. One entry per unfilled `.img-slot`.

Two kinds:

- **GENERATE** - paste the prompt into ChatGPT / an image model. These can also be produced automatically with `node scripts/generate-article-images.mjs --all` once AI credit is available, which is cheaper and keeps the whole library visually consistent.
- **CAPTURE** - take a real screenshot. These show a browser window or another product's UI, so generating them would be inaccurate.

The same prompt is also injected as an HTML comment directly above each placeholder in the article.

---

## Cloudways Alternatives: How to Pick One That Actually Fits

`cloudways-alternatives` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: A shot of your current setup spread across separate tabs (host, database, storage, CDN) next to one consolidated dashboard.
Note: Shows the "fewer moving parts" argument at a glance.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## How to Connect a Database to Your Lovable App the Right Way

`connect-a-database-to-your-lovable-app` - 2 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: DevTools open on a live site, a database string visible in the bundle. Shows why front-end code holds no secrets.
Note: Optional: annotate the leaked line in red.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

### #2 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: The live app, logged in, real rows loading from the managed database, padlock in the address bar.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## How to Add a Custom Domain and Free SSL to Your App

`custom-domain-and-ssl-for-your-app` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: The browser address bar showing your own domain with the HTTPS padlock, the payoff shot for the whole guide.
Note: Use a screenshot of your live site.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## Astro Static vs Server: Which One Are You Actually Deploying?

`deploy-astro-app` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: Your static Astro site live in the browser with a custom domain and the SSL padlock.
Note: The payoff shot. Show the green lock.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## Deploy a Claude Code App: Read the Diff, Then Ship It

`deploy-claude-code-app` - 1 slot(s)

### #1 - CAPTURE (console)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: Your Claude Code app live on its custom domain with the SSL padlock, next to the console showing a green deployment.
Note: The payoff shot.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## Deploy a Cursor App to Production: the Localhost Trap

`deploy-cursor-app` - 2 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: Browser DevTools, Network tab: the failed request to http://localhost:3000 from the deployed site.
Note: Shows the symptom readers recognize. Optional.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

### #2 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: Your Cursor app live on its custom domain, with the SSL padlock in the address bar.
Note: The payoff shot. Nothing sells "it works" like the real thing.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## How to Run a Long AI Task Without Hitting a Timeout

`deploy-long-running-ai-task-without-timeout` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: A 504 Gateway Timeout in the browser network tab, next to a request that hung for about 30 seconds before failing.
Note: Shows the exact failure a reader recognizes.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## Deploy a Lovable App to Your Own Server: the Supabase Half People Forget

`deploy-lovable-app-to-your-own-server` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: Your Lovable app live on its custom domain, logged in, with real data loading and the SSL padlock in the address bar.
Note: The payoff shot: frontend and backend talking to each other.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## Deploy a Replit App to Your Own Server: the Migration Map

`deploy-replit-app` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: Your former Repl running on its own domain with the SSL padlock, served from your own server.
Note: The "it's really off Replit now" payoff.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## How to Deploy Strapi to Production Without Losing Your Content

`deploy-strapi-app` - 3 slot(s)

### #1 - CAPTURE (third-party-ui)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows another product's own interface, so an AI image would be inaccurate and a reader could tell.
Capture: The Strapi admin content-type builder with a couple of collection types modeled.
Note: Shows what is at stake when SQLite gets wiped: your content model and entries.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

### #2 - CAPTURE (third-party-ui)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows another product's own interface, so an AI image would be inaccurate and a reader could tell.
Capture: The Strapi media library with a few images, served from S3-compatible object storage.
Note: Optional. Makes the uploads point concrete.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

### #3 - CAPTURE (console)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: The live Strapi admin login on your custom domain, over HTTPS.
Note: The payoff shot: the admin panel reachable at your public URL with a valid certificate.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## Deploy Your v0 App to a Server You Own: Start With What v0 Gave You

`deploy-v0-app` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: Your v0 app live on its own domain with the SSL padlock, real data loading behind the shadcn/ui interface.
Note: The payoff shot. Optional.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## Deploy a Windsurf App: Does It Build From a Clean Clone?

`deploy-windsurf-app` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: Your Windsurf app live on its custom domain with the SSL padlock in the address bar.
Note: The payoff shot.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## A DigitalOcean App Platform Alternative for When You Outgrow the Box

`digitalocean-app-platform-alternative` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: A PaaS component or resource list where the app plus a separate managed database plus add-on services are each metered on their own line.
Note: Grounds the add-on-sprawl point in something real. Author supplies.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## Error Establishing a Database Connection in WordPress: A Fix-It Decision Tree

`fix-error-establishing-database-connection-wordpress` - 1 slot(s)

### #1 - CAPTURE (third-party-ui)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows another product's own interface, so an AI image would be inaccurate and a reader could tell.
Capture: The WordPress database repair page at /wp-admin/maint/repair.php with the repair buttons.
Note: Enable it, run it, then remove the line.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## Free App Hosting: Your Real Options, by What You're Hosting

`free-app-hosting-options` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: A static site live on its own domain with the SSL padlock showing, plus the visit-analytics view.
Note: Shows the genuinely-free front-end path end to end.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## Free Tier vs Cheap VPS: What Actually Hosts Your Side Project?

`free-tier-vs-cheap-vps` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: A custom domain going live with SSL on the new server, to show graduating is quick and finished.
Note: Optional: the green padlock on the project's own domain.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## The GCP Dammam Region (me-central2): A Practical Guide

`gcp-dammam-region-guide` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: Your live site on its custom domain with the HTTPS padlock, served from Dammam.
Note: Proof the in-Kingdom stack is live.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## JWT Authentication: A Practical Guide for Node and Python

`jwt-authentication-guide` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: Browser devtools, Application tab, showing the auth cookie with HttpOnly and Secure both checked.
Note: Proves the token is not reachable from JavaScript.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## Kloudbean vs WP Engine: Walled Garden or Open Platform?

`kloudbean-vs-wp-engine` - 1 slot(s)

### #1 - CAPTURE (console)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: A screenshot of the sprawl: separate browser tabs for your WordPress host, your API host, your database provider, and your file storage, all open at once.
Note: Shows the "four logins" problem in one glance.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## Self-Host Your Lovable App: Own the Running App

`lovable-self-hosted-alternative` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: The Lovable app live on its own custom domain, logged in with real data loading and the SSL padlock in the address bar.
Note: The ownership payoff: your app, your domain, your data. Author supplies.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## The Namecheap Alternative for When You've Outgrown Shared Hosting

`namecheap-alternative` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: A browser showing a 508 Resource Limit Is Reached page, or a slow load in the network tab.
Note: Optional: the exact error your readers Google before they search for an alternative.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## Self-Host Langflow: Run Your AI Pipelines on a Server You Own

`self-host-langflow` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: Langflow open at your own subdomain with the SSL padlock showing, proving it runs on your infrastructure.
Note: Author screenshot.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## How to Self-Host Metabase in Production (Without the H2 Trap)

`self-host-metabase` - 1 slot(s)

### #1 - CAPTURE (third-party-ui)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows another product's own interface, so an AI image would be inaccurate and a reader could tell.
Capture: A Metabase dashboard your team actually uses, running on your own domain.
Note: Proof it's live and yours. Optional author shot.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## Self-Host n8n: The Cost Math That Changes Minds

`self-host-n8n` - 2 slot(s)

### #1 - CAPTURE (third-party-ui)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows another product's own interface, so an AI image would be inaccurate and a reader could tell.
Capture: The n8n workflow editor with a real automation: a schedule trigger feeding a couple of nodes into a Slack or HTTP action.
Note: Shows what "an execution" actually is. Optional author shot.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

### #2 - CAPTURE (third-party-ui)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows another product's own interface, so an AI image would be inaccurate and a reader could tell.
Capture: The n8n editor open on your own domain, right after the one-click deploy.
Note: The payoff shot, your automation home self-hosted.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## Self-Host Nextcloud and Own Your Team's Files

`self-host-nextcloud` - 2 slot(s)

### #1 - CAPTURE (third-party-ui)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows another product's own interface, so an AI image would be inaccurate and a reader could tell.
Capture: The Nextcloud files web interface with folders, sharing icons, and the upload button.
Note: Optional: the Drive-like view your team will actually use.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

### #2 - CAPTURE (third-party-ui)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows another product's own interface, so an AI image would be inaccurate and a reader could tell.
Capture: The Nextcloud desktop sync client showing a synced folder and its status.
Note: Optional: proves it behaves like Dropbox for end users.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## Self-Host Ollama and Open WebUI: Your Own Private AI Chat

`self-host-ollama-open-webui` - 2 slot(s)

### #1 - CAPTURE (third-party-ui)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows another product's own interface, so an AI image would be inaccurate and a reader could tell.
Capture: The Open WebUI chat running on your own domain, mid-conversation, with a model picker visible.
Note: The payoff shot: a private ChatGPT-style chat that's yours.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

### #2 - CAPTURE (console)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: Open WebUI admin panel showing user accounts and roles for a team.
Note: Proof it's a shared tool, not a single-user toy.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## How to Self-Host Rocket.Chat, the Open-Source Slack Alternative

`self-host-rocketchat` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: Browser devtools network tab showing the Rocket.Chat websocket upgraded to wss and staying open.
Note: The quickest way to confirm realtime actually works.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## How to Self-Host Strapi in Production (Without Losing Your Data)

`self-host-strapi` - 2 slot(s)

### #1 - CAPTURE (third-party-ui)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows another product's own interface, so an AI image would be inaccurate and a reader could tell.
Capture: Strapi Media Library showing uploaded files served from your S3-compatible bucket URL, not the local server.
Note: Confirms uploads are landing in object storage.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

### #2 - CAPTURE (third-party-ui)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows another product's own interface, so an AI image would be inaccurate and a reader could tell.
Capture: The Strapi admin login screen running on your own domain over HTTPS after the first deploy.
Note: Proof the self-hosted instance is live.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## How to Speed Up WooCommerce When a Cache Plugin Isn't Enough

`speed-up-woocommerce` - 1 slot(s)

### #1 - CAPTURE (third-party-ui)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows another product's own interface, so an AI image would be inaccurate and a reader could tell.
Capture: The Redis Object Cache plugin in wp-admin showing a connected, active status.
Note: Confirm the object cache is actually live, not just installed.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## SSL vs TLS: What's the Difference, and Which One Are You Actually Using?

`ssl-tls-explained` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: A browser address bar showing the padlock, with the certificate details popover open (issuer, valid dates, domain).
Note: The one place a visitor can actually see the certificate that TLS is using.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## How to Start an AI Automation Agency: An Honest Playbook

`start-an-ai-automation-agency` - 2 slot(s)

### #1 - CAPTURE (third-party-ui)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows another product's own interface, so an AI image would be inaccurate and a reader could tell.
Capture: Your automation tool open on a real client workflow: a trigger feeding a few nodes into an action.
Note: An n8n, Make, or Zapier editor screenshot works well here.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

### #2 - CAPTURE (third-party-ui)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows another product's own interface, so an AI image would be inaccurate and a reader could tell.
Capture: A client's n8n instance live on its own subdomain with the padlock showing, right after deploy.
Note: Reinforces the "each client isolated, SSL handled" point.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## What a WAF Does: 7 Attacks a Web Application Firewall Blocks

`what-a-waf-does` - 1 slot(s)

### #1 - CAPTURE (third-party-ui)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows another product's own interface, so an AI image would be inaccurate and a reader could tell.
Capture: An access log or WAF event feed with blocked probes: requests for /.env, /wp-admin, and a SQL-injection string, each marked 403.
Note: Real log lines make the "background noise" concrete.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## Why Your AI App Breaks After You Add a Custom Domain

`why-ai-app-breaks-after-custom-domain` - 1 slot(s)

### #1 - CAPTURE (console)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: The browser Network tab after the domain switch, showing requests still going to the old preview URL instead of the new domain.
Note: A real screenshot of the failing calls makes the diagnosis obvious.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

## Why Your SQLite Data Disappears After a Redeploy (and How to Stop It)

`why-sqlite-data-disappears-on-redeploy` - 1 slot(s)

### #1 - CAPTURE (browser-shot)

```
SCREENSHOT NEEDED (do not generate this one).
This slot shows a real browser window (address bar, devtools, or tabs), so an AI image would be inaccurate and a reader could tell.
Capture: A broken profile image or a 404 in the network tab after a deploy, showing local-disk uploads vanishing the same way.
Note: Reinforces that it is one pattern, not one bug.
Crop to 16:9, keep text legible at 1200px wide, and blur or fake any real credentials, tokens, IPs or customer data.
```

---

# Social posts: Environment Variables Done Right for a Deployed App

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/environment-variables-done-right/

## X / Twitter
```
Every environment variable is one of two kinds.

https://www.kloudbean.com/blog/environment-variables-done-right/
#Kloudbean #AIapps #VibeCoding #DevOps #Deployment
```

## LinkedIn
```
Every environment variable is one of two kinds.

Build-time variables (prefixed `NEXT_PUBLIC_`, `VITE_`, or `REACT_APP_`) get baked into the browser bundle when you build, so they're public and can never hold a secret. Runtime variables (`DATABASE_URL`, API keys) are read live by the server, stay off the browser, and can change without a rebuild. Set them in Runtime Configuration, keep secrets unprefixed and out of Git, and redeploy after changes.

Read the full guide: https://www.kloudbean.com/blog/environment-variables-done-right/

#Kloudbean #AIapps #VibeCoding #DevOps #Deployment
```

## X thread
```
1/6  Environment Variables Done Right for a Deployed App

Quick thread 🧵
```
```
2/6  Every environment variable is one of two kinds.
```
```
3/6  Build-time variables (prefixed `NEXT_PUBLIC_`, `VITE_`, or `REACT_APP_`) get baked into the browser bundle when you build, so they're public and can never hold a secret.
```
```
4/6  Runtime variables (`DATABASE_URL`, API keys) are read live by the server, stay off the browser, and can change without a rebuild.
```
```
5/6  Set them in Runtime Configuration, keep secrets unprefixed and out of Git, and redeploy after changes.
```
```
6/6  Full walkthrough:
https://www.kloudbean.com/blog/environment-variables-done-right/
#Kloudbean #AIapps #VibeCoding #DevOps #Deployment
```

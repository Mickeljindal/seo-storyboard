# Social posts: 505 http version not supported

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/505-http-version-not-supported/

## X / Twitter
```
> HTTP 505 means a server in the request path refuses the HTTP version it was handed.

https://www.kloudbean.com/blog/505-http-version-not-supported/
#Kloudbean #CloudHosting #DevOps #WebDev
```

## LinkedIn
```
> HTTP 505 means a server in the request path refuses the HTTP version it was handed.

A genuine 505 from an origin application is uncommon, because real servers negotiate versions and fall back to HTTP/1.1 rather than refusing outright. In practice a 505 error is emitted by a reverse proxy, a WAF rule, a CDN, or a load balancer reacting to a protocol version or a malformed request line. Reproduce it with curl while forcing a version, then again while bypassing the edge, and you will see which layer sent it.

Read the full guide: https://www.kloudbean.com/blog/505-http-version-not-supported/

#Kloudbean #CloudHosting #DevOps #WebDev
```

## X thread
```
1/6  505 http version not supported

Quick thread 🧵
```
```
2/6  > HTTP 505 means a server in the request path refuses the HTTP version it was handed.
```
```
3/6  A genuine 505 from an origin application is uncommon, because real servers negotiate versions and fall back to HTTP/1.1 rather than refusing outright.
```
```
4/6  In practice a 505 error is emitted by a reverse proxy, a WAF rule, a CDN, or a load balancer reacting to a protocol version or a malformed request line.
```
```
5/6  Reproduce it with curl while forcing a version, then again while bypassing the edge, and you will see which layer sent it.
```
```
6/6  Full walkthrough:
https://www.kloudbean.com/blog/505-http-version-not-supported/
#Kloudbean #CloudHosting #DevOps #WebDev
```

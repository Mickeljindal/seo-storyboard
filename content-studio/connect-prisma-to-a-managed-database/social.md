# Social posts: connect prisma to a managed database

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/connect-prisma-to-a-managed-database/

## X / Twitter
```
Point Prisma's datasource `url` at `env("DATABASE_URL")`, set `DATABASE_URL` as an environment variable (never in code) to your managed Postgres or MySQL…

https://www.kloudbean.com/blog/connect-prisma-to-a-managed-database/
#Kloudbean #CloudHosting #DevOps #WebDev #Database
```

## LinkedIn
```
Point Prisma's datasource `url` at `env("DATABASE_URL")`, set `DATABASE_URL` as an environment variable (never in code) to your managed Postgres or MySQL…

Cap connections with `connection_limit` in the URL so many app instances don't exhaust the database. Connect, migrate, and survive production.

Read the full guide: https://www.kloudbean.com/blog/connect-prisma-to-a-managed-database/

#Kloudbean #CloudHosting #DevOps #WebDev #Database
```

## X thread
```
1/5  connect prisma to a managed database

Quick thread 🧵
```
```
2/5  Point Prisma's datasource `url` at `env("DATABASE_URL")`, set `DATABASE_URL` as an environment variable (never in code) to your managed Postgres or MySQL connection string, then run `prisma generate` followed by `prisma migrate deploy` on every deploy.
```
```
3/5  Cap connections with `connection_limit` in the URL so many app instances don't exhaust the database.
```
```
4/5  Connect, migrate, and survive production.
```
```
5/5  Full walkthrough:
https://www.kloudbean.com/blog/connect-prisma-to-a-managed-database/
#Kloudbean #CloudHosting #DevOps #WebDev #Database
```

# Social posts: check ubuntu version

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/check-ubuntu-version/

## X / Twitter
```
> Run `cat /etc/os-release`.

https://www.kloudbean.com/blog/check-ubuntu-version/
#Kloudbean #CloudHosting #DevOps #WebDev
```

## LinkedIn
```
> Run `cat /etc/os-release`.

It works on every modern Linux distribution, needs nothing installed, and shows the version number and the codename together. `lsb_release -a` gives friendlier output but depends on a package that minimal images often omit. Do not use `uname -r` for this, because that reports the kernel version, which is a different number entirely.

Read the full guide: https://www.kloudbean.com/blog/check-ubuntu-version/

#Kloudbean #CloudHosting #DevOps #WebDev
```

## X thread
```
1/6  check ubuntu version

Quick thread 🧵
```
```
2/6  > Run `cat /etc/os-release`.
```
```
3/6  It works on every modern Linux distribution, needs nothing installed, and shows the version number and the codename together.
```
```
4/6  `lsb_release -a` gives friendlier output but depends on a package that minimal images often omit.
```
```
5/6  Do not use `uname -r` for this, because that reports the kernel version, which is a different number entirely.
```
```
6/6  Full walkthrough:
https://www.kloudbean.com/blog/check-ubuntu-version/
#Kloudbean #CloudHosting #DevOps #WebDev
```

# Social posts: database indexing explained

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/database-indexing-explained/

## X / Twitter
```
> An index is a sorted lookup structure, usually a B-tree, that lets the database jump straight to the rows you asked for instead of reading the whole table.

https://www.kloudbean.com/blog/database-indexing-explained/
#Kloudbean #CloudHosting #DevOps #WebDev #Database
```

## LinkedIn
```
> An index is a sorted lookup structure, usually a B-tree, that lets the database jump straight to the rows you asked for instead of reading the whole table.

Put one on the columns you filter and join on, and a query that scanned a million rows can touch a handful. The catch: every index slightly slows writes and uses disk, so you index for the queries you actually run, then use `EXPLAIN ANALYZE` to prove it worked.

Read the full guide: https://www.kloudbean.com/blog/database-indexing-explained/

#Kloudbean #CloudHosting #DevOps #WebDev #Database
```

## X thread
```
1/5  database indexing explained

Quick thread 🧵
```
```
2/5  > An index is a sorted lookup structure, usually a B-tree, that lets the database jump straight to the rows you asked for instead of reading the whole table.
```
```
3/5  Put one on the columns you filter and join on, and a query that scanned a million rows can touch a handful.
```
```
4/5  The catch: every index slightly slows writes and uses disk, so you index for the queries you actually run, then use `EXPLAIN ANALYZE` to prove it worked.
```
```
5/5  Full walkthrough:
https://www.kloudbean.com/blog/database-indexing-explained/
#Kloudbean #CloudHosting #DevOps #WebDev #Database
```

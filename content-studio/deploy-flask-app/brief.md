# Brief — Deploy a Flask App (quickstart + reference)

Cluster 3. Primary kw: deploy flask app / flask production server / gunicorn flask. Intent: how-to.
FORMAT: Quickstart + reference — a fast minimal "10-minute" numbered path first, then a reference you return to (WSGI callable app:app, workers/threads formula, why not flask run / debug=True, static, DB, secrets). Different skeleton from laravel/django/golang/rails. Opener = definition of Flask as a micro-framework → "what runs it? Gunicorn."
Include a small reference table (table.cmp) of common gunicorn start options.
Honesty woven: Python/Linux; managed=server/stack/SSL/backups, you own the app; flask run is dev-only. Not a dedicated identical honesty block.
Dashboard: add-server.png (provision a small box — Flask apps are light). Distinct byline: "Kloudbean · Flask and Gunicorn, quietly kept alive."
Slug: deploy-flask-app. Links: pillar, deploy-fastapi-app, add-managed-database, fix-503.

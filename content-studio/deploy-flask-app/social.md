# Social posts: Deploy Flask in 10 Minutes — Then the Reference You'll Keep

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/deploy-flask-app/

## X / Twitter
```
Add `gunicorn` to `requirements.txt`, then start your Flask app with `gunicorn app:app --bind 0.0.0.0:$PORT` instead of `flask run`.

https://www.kloudbean.com/blog/deploy-flask-app/
#Kloudbean #CloudHosting #DevOps #WebDev #Python #Deployment
```

## LinkedIn
```
Add `gunicorn` to `requirements.txt`, then start your Flask app with `gunicorn app:app --bind 0.0.0.0:$PORT` instead of `flask run`.

Gunicorn runs a master process plus several sync workers, so it handles real concurrent traffic and restarts a worker if one dies. Bind the port the platform assigns, set your secrets as env vars, and you're done.

Read the full guide: https://www.kloudbean.com/blog/deploy-flask-app/

#Kloudbean #CloudHosting #DevOps #WebDev #Python #Deployment
```

## X thread
```
1/5  Deploy Flask in 10 Minutes — Then the Reference You'll Keep

Quick thread 🧵
```
```
2/5  Add `gunicorn` to `requirements.txt`, then start your Flask app with `gunicorn app:app --bind 0.0.0.0:$PORT` instead of `flask run`.
```
```
3/5  Gunicorn runs a master process plus several sync workers, so it handles real concurrent traffic and restarts a worker if one dies.
```
```
4/5  Bind the port the platform assigns, set your secrets as env vars, and you're done.
```
```
5/5  Full walkthrough:
https://www.kloudbean.com/blog/deploy-flask-app/
#Kloudbean #CloudHosting #DevOps #WebDev #Python #Deployment
```

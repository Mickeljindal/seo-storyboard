# Social posts: migrate fly to kloudbean

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/migrate-fly-to-kloudbean/

## X / Twitter
```
> Inventory every Machine, volume, snapshot, and region in your Fly apps, then export your secrets with `fly secrets list` and your data with `pg_dump`.

https://www.kloudbean.com/blog/migrate-fly-to-kloudbean/
#Kloudbean #CloudHosting #DevOps #WebDev
```

## LinkedIn
```
> Inventory every Machine, volume, snapshot, and region in your Fly apps, then export your secrets with `fly secrets list` and your data with `pg_dump`.

Create the equivalent on Kloudbean: one always-on server running your app and workers under PM2, plus a managed database. Copy volume contents deliberately, since volumes are local to a host and never replicated for you. Verify on the temporary URL, cut DNS over in a quiet window, then destroy the Fly volumes and apps, because they keep billing until you do.

Read the full guide: https://www.kloudbean.com/blog/migrate-fly-to-kloudbean/

#Kloudbean #CloudHosting #DevOps #WebDev
```

## X thread
```
1/6  migrate fly to kloudbean

Quick thread 🧵
```
```
2/6  > Inventory every Machine, volume, snapshot, and region in your Fly apps, then export your secrets with `fly secrets list` and your data with `pg_dump`.
```
```
3/6  Create the equivalent on Kloudbean: one always-on server running your app and workers under PM2, plus a managed database.
```
```
4/6  Copy volume contents deliberately, since volumes are local to a host and never replicated for you.
```
```
5/6  Verify on the temporary URL, cut DNS over in a quiet window, then destroy the Fly volumes and apps, because they keep billing until you do.
```
```
6/6  Full walkthrough:
https://www.kloudbean.com/blog/migrate-fly-to-kloudbean/
#Kloudbean #CloudHosting #DevOps #WebDev
```

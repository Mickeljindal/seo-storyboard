# Brief — Managed PostgreSQL Hosting (Q&A explainer)

Cluster 7. Primary kw: managed postgresql / postgres hosting / is postgresql free. Intent: informational/commercial. Signals: "managed postgresql" 320, "postgres hosting" 320, "is postgresql free" 480, "postgresql database" 1600.
FORMAT: Q&A explainer (H2s = real questions). Not adjacent to other Q&A. Opener = definition.
Questions: what is managed PostgreSQL? / is PostgreSQL free (yes, open source; you pay for hosting/management)? / how do I connect my app (DATABASE_URL — SCREENSHOT env-vars.png)? / what does the platform manage vs me? / do I need Postgres or MySQL (brief, link vs)? / can I use extensions (PostGIS, pgvector)? / how does it scale (bigger instance, read replicas)?
Honesty woven: Linux; Postgres itself free/open-source; managed=engine/patching/backups/HA; you own schema+data+queries; exportable (pg_dump).
Byline: "Kloudbean · Postgres you don't have to patch."
Slug: managed-postgresql-hosting. Links: managed-mysql-hosting, mysql-vs-postgresql, database-read-replicas-scaling, add-managed-database (C1).

# Social posts: subuser and uac guide

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/subuser-and-uac-guide/

## X / Twitter
```
> A subuser is a separate login under your account with its own credentials, so nobody shares a password.

https://www.kloudbean.com/blog/subuser-and-uac-guide/
#Kloudbean #CloudHosting #DevOps #WebDev
```

## LinkedIn
```
> A subuser is a separate login under your account with its own credentials, so nobody shares a password.

User Access Control (UAC) is what decides what that subuser can actually do, and the key idea is that permission is two-dimensional: it is granted per-resource (which app, which database, which bucket) and per-action (view, deploy, restart, delete, manage billing) at the same time. So instead of one master password that can do everything, each person gets exactly the cells in that grid they need. For an agency, that means a client can watch their own site without being able to break it, a…

Read the full guide: https://www.kloudbean.com/blog/subuser-and-uac-guide/

#Kloudbean #CloudHosting #DevOps #WebDev
```

## X thread
```
1/6  subuser and uac guide

Quick thread 🧵
```
```
2/6  > A subuser is a separate login under your account with its own credentials, so nobody shares a password.
```
```
3/6  User Access Control (UAC) is what decides what that subuser can actually do, and the key idea is that permission is two-dimensional: it is granted per-resource (which app, which database, which bucket) and per-action (view, deploy, restart, delete, manage billing) at the same…
```
```
4/6  So instead of one master password that can do everything, each person gets exactly the cells in that grid they need.
```
```
5/6  For an agency, that means a client can watch their own site without being able to break it, a contractor can deploy one app and nothing else, and offboarding someone is one switch rather than a company-wide password reset.
```
```
6/6  Full walkthrough:
https://www.kloudbean.com/blog/subuser-and-uac-guide/
#Kloudbean #CloudHosting #DevOps #WebDev
```

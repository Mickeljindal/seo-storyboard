# Social posts: How to Deploy a NestJS App to Production, Start to Finish

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/deploy-nestjs-app/

## X / Twitter
```
How to Deploy a NestJS App to Production, Start to Finish

https://www.kloudbean.com/blog/deploy-nestjs-app/
#Kloudbean #CloudHosting #DevOps #WebDev #Deployment
```

## LinkedIn
```
How to Deploy a NestJS App to Production, Start to Finish

Run `nest build` to compile TypeScript into `dist/`, set `NODE_ENV=production`, and start the app with `node dist/main.js` (not `start:dev`). Read config through `@nestjs/config` from environment variables, call `app.enableShutdownHooks()` so redeploys close cleanly, and run the process under PM2 behind Nginx with SSL. On Kloudbean you connect a Git repo, set install/build/start commands, add env vars in the UI, and every push builds and ships with live logs.

Read the full guide: https://www.kloudbean.com/blog/deploy-nestjs-app/

#Kloudbean #CloudHosting #DevOps #WebDev #Deployment
```

## X thread
```
1/5  How to Deploy a NestJS App to Production, Start to Finish

Quick thread 🧵
```
```
2/5  Run `nest build` to compile TypeScript into `dist/`, set `NODE_ENV=production`, and start the app with `node dist/main.js` (not `start:dev`).
```
```
3/5  Read config through `@nestjs/config` from environment variables, call `app.enableShutdownHooks()` so redeploys close cleanly, and run the process under PM2 behind Nginx with SSL.
```
```
4/5  On Kloudbean you connect a Git repo, set install/build/start commands, add env vars in the UI, and every push builds and ships with live logs.
```
```
5/5  Full walkthrough:
https://www.kloudbean.com/blog/deploy-nestjs-app/
#Kloudbean #CloudHosting #DevOps #WebDev #Deployment
```

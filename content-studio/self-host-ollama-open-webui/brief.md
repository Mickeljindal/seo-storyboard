# Brief — Self-Host Ollama + Open WebUI (quickstart + reference)

Cluster 2. Primary kw: self host ollama / open webui / private chatgpt / self hosted ai chat (open webui has rival consensus). Intent: how-to.
FORMAT: Quickstart + reference (distinct from decision-guide/listicle/cost-breakdown/tutorial used so far). Opener = privacy scenario (prompts leaving your company). Part 1 = fast path to a working private chat. Part 2 = reference (model sizes vs RAM/GPU, CPU-only is fine-but-slow, connecting WebUI->Ollama, adding team users). HONEST: this is the one self-host tool where hardware matters a lot — small models (7B) run on CPU with ~8GB RAM but slowly; GPU is much faster; big models need big memory. Prompts/data stay on your server (the whole point).
Dashboard: add-server.png (sizing/GPU is the crux).
Slug: self-host-ollama-open-webui. Links: best-self-hosted-tools, self-host-langflow, pricing. Distinct byline.

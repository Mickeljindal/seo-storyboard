# Social posts: How to Deploy a Next.js App to Your Own Server

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/deploy-nextjs-app-to-your-own-server/

## X / Twitter
```
How to Deploy a Next.js App to Your Own Server

https://www.kloudbean.com/blog/deploy-nextjs-app-to-your-own-server/
#Kloudbean #AIapps #VibeCoding #DevOps #NextJS #Deployment
```

## LinkedIn
```
How to Deploy a Next.js App to Your Own Server

A built Next.js app is a running Node process (`next build` then `next start`), not a static folder, unless you deliberately set `output: 'export'`. Deploy it like any Node app: launch a server, connect your repo, set Build to `next build` and Start to `next start`, listen on `process.env.PORT`. SSG, SSR, and ISR then all work, because each one is just a different thing that same process does.

Read the full guide: https://www.kloudbean.com/blog/deploy-nextjs-app-to-your-own-server/

#Kloudbean #AIapps #VibeCoding #DevOps #NextJS #Deployment
```

## X thread
```
1/5  How to Deploy a Next.js App to Your Own Server

Quick thread 🧵
```
```
2/5  A built Next.js app is a running Node process (`next build` then `next start`), not a static folder, unless you deliberately set `output: 'export'`.
```
```
3/5  Deploy it like any Node app: launch a server, connect your repo, set Build to `next build` and Start to `next start`, listen on `process.env.PORT`.
```
```
4/5  SSG, SSR, and ISR then all work, because each one is just a different thing that same process does.
```
```
5/5  Full walkthrough:
https://www.kloudbean.com/blog/deploy-nextjs-app-to-your-own-server/
#Kloudbean #AIapps #VibeCoding #DevOps #NextJS #Deployment
```

# Social posts: fix cannot find module node

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/fix-cannot-find-module-node/

## X / Twitter
```
> First decide what's missing.

https://www.kloudbean.com/blog/fix-cannot-find-module-node/
#Kloudbean #CloudHosting #DevOps #WebDev
```

## LinkedIn
```
> First decide what's missing.

If it's a package name like `express`, the module isn't installed, run `npm install` and confirm it's in `dependencies`, not `devDependencies`. If it's a relative path like `./routes/user`, the path, the filename case, or a missing ESM `.js` extension is wrong. If it only breaks in production, suspect case sensitivity on Linux or a build step that didn't run.

Read the full guide: https://www.kloudbean.com/blog/fix-cannot-find-module-node/

#Kloudbean #CloudHosting #DevOps #WebDev
```

## X thread
```
1/6  fix cannot find module node

Quick thread 🧵
```
```
2/6  > First decide what's missing.
```
```
3/6  If it's a package name like `express`, the module isn't installed, run `npm install` and confirm it's in `dependencies`, not `devDependencies`.
```
```
4/6  If it's a relative path like `./routes/user`, the path, the filename case, or a missing ESM `.js` extension is wrong.
```
```
5/6  If it only breaks in production, suspect case sensitivity on Linux or a build step that didn't run.
```
```
6/6  Full walkthrough:
https://www.kloudbean.com/blog/fix-cannot-find-module-node/
#Kloudbean #CloudHosting #DevOps #WebDev
```

# Social posts: fix node app crashing on deploy

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/fix-node-app-crashing-on-deploy/

## X / Twitter
```
> Because production differs from your machine in ways your code assumed away.

https://www.kloudbean.com/blog/fix-node-app-crashing-on-deploy/
#Kloudbean #CloudHosting #DevOps #WebDev #Deployment
```

## LinkedIn
```
> Because production differs from your machine in ways your code assumed away.

The frequent causes: a missing environment variable, a build or start command that didn't run, a dependency that lives in `devDependencies`, a filename case bug that only breaks on Linux, an app not reading `process.env.PORT`, or a database that's unreachable from production. Read the deploy logs first, they name the error, then reproduce the production setup locally to confirm the fix.

Read the full guide: https://www.kloudbean.com/blog/fix-node-app-crashing-on-deploy/

#Kloudbean #CloudHosting #DevOps #WebDev #Deployment
```

## X thread
```
1/5  fix node app crashing on deploy

Quick thread 🧵
```
```
2/5  > Because production differs from your machine in ways your code assumed away.
```
```
3/5  The frequent causes: a missing environment variable, a build or start command that didn't run, a dependency that lives in `devDependencies`, a filename case bug that only breaks on Linux, an app not reading `process.env.PORT`, or a database that's unreachable from production.
```
```
4/5  Read the deploy logs first, they name the error, then reproduce the production setup locally to confirm the fix.
```
```
5/5  Full walkthrough:
https://www.kloudbean.com/blog/fix-node-app-crashing-on-deploy/
#Kloudbean #CloudHosting #DevOps #WebDev #Deployment
```

# Social posts: self host nocodb

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/self-host-nocodb/

## X / Twitter
```
Run the NocoDB Node app on a managed Node server.

https://www.kloudbean.com/blog/self-host-nocodb/
#Kloudbean #CloudHosting #DevOps #WebDev
```

## LinkedIn
```
Run the NocoDB Node app on a managed Node server.

Back its metadata database with a managed MySQL or PostgreSQL instead of the default SQLite, and set that connection through the `NC_DB` environment variable. Set `NC_AUTH_JWT_SECRET` so logins survive a restart, put SSL in front, and lock the database to your app server's IP. Then add your existing SQL databases as data sources so NocoDB puts a spreadsheet UI on tables you already run.

Read the full guide: https://www.kloudbean.com/blog/self-host-nocodb/

#Kloudbean #CloudHosting #DevOps #WebDev
```

## X thread
```
1/6  self host nocodb

Quick thread 🧵
```
```
2/6  Run the NocoDB Node app on a managed Node server.
```
```
3/6  Back its metadata database with a managed MySQL or PostgreSQL instead of the default SQLite, and set that connection through the `NC_DB` environment variable.
```
```
4/6  Set `NC_AUTH_JWT_SECRET` so logins survive a restart, put SSL in front, and lock the database to your app server's IP.
```
```
5/6  Then add your existing SQL databases as data sources so NocoDB puts a spreadsheet UI on tables you already run.
```
```
6/6  Full walkthrough:
https://www.kloudbean.com/blog/self-host-nocodb/
#Kloudbean #CloudHosting #DevOps #WebDev
```

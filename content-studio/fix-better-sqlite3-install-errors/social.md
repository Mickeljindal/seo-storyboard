# Social posts: fix better sqlite3 install errors

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/fix-better-sqlite3-install-errors/

## X / Twitter
```
> The binary was compiled for a different Node.js version than the one running it.

https://www.kloudbean.com/blog/fix-better-sqlite3-install-errors/
#Kloudbean #CloudHosting #DevOps #WebDev #Database
```

## LinkedIn
```
> The binary was compiled for a different Node.js version than the one running it.

Rebuild it against your current runtime with `npm rebuild better-sqlite3 --build-from-source`, or delete `node_modules` and run a fresh `npm ci` on the machine that will actually run the app. To stop it recurring, pin your Node major version in `package.json` engines, never copy `node_modules` between machines, and install dependencies on the target platform rather than shipping them.

Read the full guide: https://www.kloudbean.com/blog/fix-better-sqlite3-install-errors/

#Kloudbean #CloudHosting #DevOps #WebDev #Database
```

## X thread
```
1/5  fix better sqlite3 install errors

Quick thread 🧵
```
```
2/5  > The binary was compiled for a different Node.js version than the one running it.
```
```
3/5  Rebuild it against your current runtime with `npm rebuild better-sqlite3 --build-from-source`, or delete `node_modules` and run a fresh `npm ci` on the machine that will actually run the app.
```
```
4/5  To stop it recurring, pin your Node major version in `package.json` engines, never copy `node_modules` between machines, and install dependencies on the target platform rather than shipping them.
```
```
5/5  Full walkthrough:
https://www.kloudbean.com/blog/fix-better-sqlite3-install-errors/
#Kloudbean #CloudHosting #DevOps #WebDev #Database
```

# Social posts: Git Delete Branch and Rename Branch: Local and Remote, One Reference

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/git-delete-and-rename-branch/

## X / Twitter
```
To delete a local branch, run git branch -d name, or -D to force one that is not merged.

https://www.kloudbean.com/blog/git-delete-and-rename-branch/
#Kloudbean #CloudHosting #DevOps #WebDev #Deployment
```

## LinkedIn
```
To delete a local branch, run git branch -d name, or -D to force one that is not merged.

To delete a remote branch, run git push origin --delete name. To rename locally, use git branch -m old new. There is no remote rename: push the new name, delete the old one, then reset tracking with git push -u origin new.

Read the full guide: https://www.kloudbean.com/blog/git-delete-and-rename-branch/

#Kloudbean #CloudHosting #DevOps #WebDev #Deployment
```

## X thread
```
1/6  Git Delete Branch and Rename Branch: Local and Remote, One Reference

Quick thread 🧵
```
```
2/6  To delete a local branch, run git branch -d name, or -D to force one that is not merged.
```
```
3/6  To delete a remote branch, run git push origin --delete name.
```
```
4/6  To rename locally, use git branch -m old new.
```
```
5/6  There is no remote rename: push the new name, delete the old one, then reset tracking with git push -u origin new.
```
```
6/6  Full walkthrough:
https://www.kloudbean.com/blog/git-delete-and-rename-branch/
#Kloudbean #CloudHosting #DevOps #WebDev #Deployment
```

# Brief — Self-Host GitLab (decision guide)

Cluster 2 (Self-Hosted Tools). Primary kw: self-host gitlab / gitlab self-hosted (7-rival consensus in SEMrush gap data; also "self managed gitlab", "gitlab self-hosted pricing"). Intent: commercial/consideration.
FORMAT: Decision guide (NOT a 6-step deploy template). Opener = seat-pricing reality. Headers phrased as decisions/questions. TL;DR box. Cost table (GitLab.com seats vs one self-managed server — illustrative). Weave honesty naturally (no dedicated 'honest limits'+'wrapping up' pair).
Angle: GitLab Free self-managed removes per-seat cost of paid SaaS tiers; you pay for one server. GitLab is RAM-heavy (4GB min, 8GB comfortable) — sizing is the real decision, so embed add-server.png. Cover: who should/shouldn't, what you run (repos+CI+registry+Postgres), CI runners resource note, backups/updates = your job vs platform keeps box healthy. Honesty: Linux; managed=server/stack/SSL/backups, you own GitLab + data.
Dashboard: add-server.png (sizing is the crux). Data-driven: mention rivals rank for this.
Slug: self-host-gitlab. Links: pillar deploy guide, add-managed-database (once written), pricing. Distinct byline/closing.

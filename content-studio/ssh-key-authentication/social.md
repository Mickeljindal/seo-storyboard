# Social posts: ssh key authentication

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/ssh-key-authentication/

## X / Twitter
```
Generate an ed25519 key with `ssh-keygen -t ed25519`, copy the public key into the server's `~/.ssh/authorized_keys` with `ssh-copy-id`, confirm you can log…

https://www.kloudbean.com/blog/ssh-key-authentication/
#Kloudbean #CloudHosting #DevOps #WebDev
```

## LinkedIn
```
Generate an ed25519 key with `ssh-keygen -t ed25519`, copy the public key into the server's `~/.ssh/authorized_keys` with `ssh-copy-id`, confirm you can log…

Your private key never leaves your laptop and proves your identity through a challenge an attacker cannot replay. On a Kloudbean server, Shorewall and Fail2ban already guard port 22; keys are the part you bring.

Read the full guide: https://www.kloudbean.com/blog/ssh-key-authentication/

#Kloudbean #CloudHosting #DevOps #WebDev
```

## X thread
```
1/5  ssh key authentication

Quick thread 🧵
```
```
2/5  Generate an ed25519 key with `ssh-keygen -t ed25519`, copy the public key into the server's `~/.ssh/authorized_keys` with `ssh-copy-id`, confirm you can log in with no password, then set `PasswordAuthentication no` and reload sshd.
```
```
3/5  Your private key never leaves your laptop and proves your identity through a challenge an attacker cannot replay.
```
```
4/5  On a Kloudbean server, Shorewall and Fail2ban already guard port 22; keys are the part you bring.
```
```
5/5  Full walkthrough:
https://www.kloudbean.com/blog/ssh-key-authentication/
#Kloudbean #CloudHosting #DevOps #WebDev
```

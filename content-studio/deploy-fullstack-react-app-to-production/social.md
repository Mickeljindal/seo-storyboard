# Social posts: How to Deploy a Full-Stack React App to Production

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/deploy-fullstack-react-app-to-production/

## X / Twitter
```
How to Deploy a Full-Stack React App to Production

https://www.kloudbean.com/blog/deploy-fullstack-react-app-to-production/
#Kloudbean #AIapps #VibeCoding #DevOps #React #Deployment
```

## LinkedIn
```
How to Deploy a Full-Stack React App to Production

To deploy a full-stack React app, decide first: one process (your Node/Express server serves the built SPA *and* the `/api` routes, one domain, no CORS) or two apps (static SPA on `app.`, API on `api.`, sharing a database). Default to one process. Then fix the two SPA gotchas: a catch-all route so refreshing an inner page doesn't 404, and an API base URL that's relative or env-configured, never hard-coded to localhost.

Read the full guide: https://www.kloudbean.com/blog/deploy-fullstack-react-app-to-production/

#Kloudbean #AIapps #VibeCoding #DevOps #React #Deployment
```

## X thread
```
1/5  How to Deploy a Full-Stack React App to Production

Quick thread 🧵
```
```
2/5  To deploy a full-stack React app, decide first: one process (your Node/Express server serves the built SPA *and* the `/api` routes, one domain, no CORS) or two apps (static SPA on `app.`, API on `api.`, sharing a database).
```
```
3/5  Default to one process.
```
```
4/5  Then fix the two SPA gotchas: a catch-all route so refreshing an inner page doesn't 404, and an API base URL that's relative or env-configured, never hard-coded to localhost.
```
```
5/5  Full walkthrough:
https://www.kloudbean.com/blog/deploy-fullstack-react-app-to-production/
#Kloudbean #AIapps #VibeCoding #DevOps #React #Deployment
```

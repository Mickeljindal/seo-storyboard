# Brief — WP-CLI Guide (quickstart + reference)

Cluster 6. Primary kw: wordpress cli / wp-cli / wp cli commands. Intent: how-to/reference. Signal 720.
FORMAT: Quickstart + reference (fast intro, then command reference table). Not adjacent to other quickstart. Opener = definition (manage WP from terminal, no clicking).
Sections: what WP-CLI is / quickstart (5 commands that save time now) / reference (core+updates, plugins/themes, database export/import, SEARCH-REPLACE for migrations, users, cache) as table.cmp / the migration superpower (wp search-replace serialization-safe URL swap) / where it runs (SSH on your managed server — SCREENSHOT git-deployment.png as the deploy/SSH context) / honest note.
Real: "wordpress cli" 720.
Honesty woven: Linux; managed server gives you SSH+WP-CLI; you own the site; be careful with db commands (back up first).
Byline: "Kloudbean · WordPress, one command at a time."
Slug: wordpress-cli-guide. Links: how-to-clear-wordpress-cache, headless-wordpress-hosting, how-to-migrate-hosting-zero-downtime (C4).

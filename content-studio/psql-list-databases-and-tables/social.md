# Social posts: Postgres List Tables and Databases in psql, Organised by Task

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/psql-list-databases-and-tables/

## X / Twitter
```
Inside psql, `\l` (or `\list`) lists every database, and `\dt` lists the tables in the database you are connected to.

https://www.kloudbean.com/blog/psql-list-databases-and-tables/
#Kloudbean #AIapps #VibeCoding #DevOps #PostgreSQL #RAG
```

## LinkedIn
```
Inside psql, `\l` (or `\list`) lists every database, and `\dt` lists the tables in the database you are connected to.

Here is the catch that trips almost everyone up at least once: `\dt` only shows tables in the schemas on your `search_path`. If it comes back empty, run `\dt *.*` to see every schema, or `\dt billing.*` for one named schema. Prefer SQL?

Read the full guide: https://www.kloudbean.com/blog/psql-list-databases-and-tables/

#Kloudbean #AIapps #VibeCoding #DevOps #PostgreSQL #RAG
```

## X thread
```
1/6  Postgres List Tables and Databases in psql, Organised by Task

Quick thread 🧵
```
```
2/6  Inside psql, `\l` (or `\list`) lists every database, and `\dt` lists the tables in the database you are connected to.
```
```
3/6  Here is the catch that trips almost everyone up at least once: `\dt` only shows tables in the schemas on your `search_path`.
```
```
4/6  If it comes back empty, run `\dt *.*` to see every schema, or `\dt billing.*` for one named schema.
```
```
5/6  Prefer SQL?
```
```
6/6  Full walkthrough:
https://www.kloudbean.com/blog/psql-list-databases-and-tables/
#Kloudbean #AIapps #VibeCoding #DevOps #PostgreSQL #RAG
```

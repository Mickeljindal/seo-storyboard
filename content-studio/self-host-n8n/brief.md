# Brief — Self-Host n8n (cost-breakdown format)

Cluster 2. Primary kw: self host n8n / n8n self hosted / n8n pricing. Intent: consideration.
FORMAT: Cost-breakdown / numbers piece (distinct from decision-guide + listicle already written). Opener = per-execution pricing sting. Cost table: n8n Cloud (execution-limited tiers) vs one small server (unlimited executions). Then when-self-host-wins/when-cloud-fine, what n8n needs (light ~1GB, Postgres for production, encryption key + webhook URL env vars), brief setup shape, the real gotcha (N8N_ENCRYPTION_KEY + WEBHOOK_URL). Weave honesty. Distinct closing/byline.
Dashboard: launch-database.png (n8n uses Postgres in production; SQLite is default but not for real use).
Data note: rivals rank for n8n/self-host terms. Keep numbers illustrative.
Slug: self-host-n8n. Links: best-self-hosted-tools, add-managed-database (once written), pricing.

# Social posts: Nginx Reverse Proxy for Node.js: A Real Production Config

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/nginx-reverse-proxy-for-node/

## X / Twitter
```
> Run Node on `localhost:3000` and put nginx in front on 443.

https://www.kloudbean.com/blog/nginx-reverse-proxy-for-node/
#Kloudbean #CloudHosting #DevOps #WebDev #NodeJS
```

## LinkedIn
```
> Run Node on `localhost:3000` and put nginx in front on 443.

nginx terminates TLS, serves your static files, and proxies everything else with `proxy_pass` to `127.0.0.1:3000`, forwarding `Host`, `X-Real-IP`, `X-Forwarded-For`, and `X-Forwarded-Proto` so your app sees the real client. Add the `Upgrade` and `Connection` headers and WebSockets work too. On managed hosting the platform runs this proxy and the SSL for you.

Read the full guide: https://www.kloudbean.com/blog/nginx-reverse-proxy-for-node/

#Kloudbean #CloudHosting #DevOps #WebDev #NodeJS
```

## X thread
```
1/6  Nginx Reverse Proxy for Node.js: A Real Production Config

Quick thread 🧵
```
```
2/6  > Run Node on `localhost:3000` and put nginx in front on 443.
```
```
3/6  nginx terminates TLS, serves your static files, and proxies everything else with `proxy_pass` to `127.0.0.1:3000`, forwarding `Host`, `X-Real-IP`, `X-Forwarded-For`, and `X-Forwarded-Proto` so your app sees the real client.
```
```
4/6  Add the `Upgrade` and `Connection` headers and WebSockets work too.
```
```
5/6  On managed hosting the platform runs this proxy and the SSL for you.
```
```
6/6  Full walkthrough:
https://www.kloudbean.com/blog/nginx-reverse-proxy-for-node/
#Kloudbean #CloudHosting #DevOps #WebDev #NodeJS
```

# Social posts: connect mongoose to mongodb

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/connect-mongoose-to-mongodb/

## X / Twitter
```
Call `mongoose.connect(process.env.MONGODB_URI, { maxPoolSize: 10, serverSelectionTimeoutMS: 5000 })` once at startup, never inside a route.

https://www.kloudbean.com/blog/connect-mongoose-to-mongodb/
#Kloudbean #CloudHosting #DevOps #WebDev
```

## LinkedIn
```
Call `mongoose.connect(process.env.MONGODB_URI, { maxPoolSize: 10, serverSelectionTimeoutMS: 5000 })` once at startup, never inside a route.

Read the connection string from a `MONGODB_URI` environment variable, not from code. Attach `connection.on('error')` and `'connected'` handlers so failures are loud. Turn `autoIndex` off in production and build indexes on purpose.

Read the full guide: https://www.kloudbean.com/blog/connect-mongoose-to-mongodb/

#Kloudbean #CloudHosting #DevOps #WebDev
```

## X thread
```
1/6  connect mongoose to mongodb

Quick thread 🧵
```
```
2/6  Call `mongoose.connect(process.env.MONGODB_URI, { maxPoolSize: 10, serverSelectionTimeoutMS: 5000 })` once at startup, never inside a route.
```
```
3/6  Read the connection string from a `MONGODB_URI` environment variable, not from code.
```
```
4/6  Attach `connection.on('error')` and `'connected'` handlers so failures are loud.
```
```
5/6  Turn `autoIndex` off in production and build indexes on purpose.
```
```
6/6  Full walkthrough:
https://www.kloudbean.com/blog/connect-mongoose-to-mongodb/
#Kloudbean #CloudHosting #DevOps #WebDev
```

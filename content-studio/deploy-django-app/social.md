# Social posts: Deploy a Django App: The Production Checklist

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/deploy-django-app/

## X / Twitter
```
Set `DEBUG=False`, read `SECRET_KEY` and the database from the environment, list your domain in `ALLOWED_HOSTS`.

https://www.kloudbean.com/blog/deploy-django-app/
#Kloudbean #CloudHosting #DevOps #WebDev #Python #Deployment
```

## LinkedIn
```
Set `DEBUG=False`, read `SECRET_KEY` and the database from the environment, list your domain in `ALLOWED_HOSTS`.

Then deploy: `pip install`, `collectstatic`, `migrate`, and start with `gunicorn` bound to the assigned port. That's the whole job. `runserver` never touches production.

Read the full guide: https://www.kloudbean.com/blog/deploy-django-app/

#Kloudbean #CloudHosting #DevOps #WebDev #Python #Deployment
```

## X thread
```
1/6  Deploy a Django App: The Production Checklist

Quick thread 🧵
```
```
2/6  Set `DEBUG=False`, read `SECRET_KEY` and the database from the environment, list your domain in `ALLOWED_HOSTS`.
```
```
3/6  Then deploy: `pip install`, `collectstatic`, `migrate`, and start with `gunicorn` bound to the assigned port.
```
```
4/6  That's the whole job.
```
```
5/6  `runserver` never touches production.
```
```
6/6  Full walkthrough:
https://www.kloudbean.com/blog/deploy-django-app/
#Kloudbean #CloudHosting #DevOps #WebDev #Python #Deployment
```

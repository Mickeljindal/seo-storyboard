# Social posts: migrate vercel api to kloudbean

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/migrate-vercel-api-to-kloudbean/

## X / Twitter
```
> Keep the frontend where it is and move the backend to a persistent Node server.

https://www.kloudbean.com/blog/migrate-vercel-api-to-kloudbean/
#Kloudbean #CloudHosting #DevOps #WebDev
```

## LinkedIn
```
> Keep the frontend where it is and move the backend to a persistent Node server.

Convert your Vercel route handlers into Express (or NestJS/Fastify) routes, host the API on its own subdomain like `api.example.com`, and update the frontend to call that base URL. Then handle the three things that actually break: CORS (allow your frontend's origin), cookies and auth across origins, and the database connection string. Migrate endpoint by endpoint rather than all at once, starting with the slowest ones.

Read the full guide: https://www.kloudbean.com/blog/migrate-vercel-api-to-kloudbean/

#Kloudbean #CloudHosting #DevOps #WebDev
```

## X thread
```
1/6  migrate vercel api to kloudbean

Quick thread 🧵
```
```
2/6  > Keep the frontend where it is and move the backend to a persistent Node server.
```
```
3/6  Convert your Vercel route handlers into Express (or NestJS/Fastify) routes, host the API on its own subdomain like `api.example.com`, and update the frontend to call that base URL.
```
```
4/6  Then handle the three things that actually break: CORS (allow your frontend's origin), cookies and auth across origins, and the database connection string.
```
```
5/6  Migrate endpoint by endpoint rather than all at once, starting with the slowest ones.
```
```
6/6  Full walkthrough:
https://www.kloudbean.com/blog/migrate-vercel-api-to-kloudbean/
#Kloudbean #CloudHosting #DevOps #WebDev
```

# Social posts: connect drizzle to postgres

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/connect-drizzle-to-postgres/

## X / Twitter
```
Pick a driver (node-postgres, the `pg` Pool, or postgres.js), read your `DATABASE_URL` from an environment variable, and wrap the client in `drizzle()`.

https://www.kloudbean.com/blog/connect-drizzle-to-postgres/
#Kloudbean #CloudHosting #DevOps #WebDev #PostgreSQL
```

## LinkedIn
```
Pick a driver (node-postgres, the `pg` Pool, or postgres.js), read your `DATABASE_URL` from an environment variable, and wrap the client in `drizzle()`.

Define your tables in a TypeScript schema, then run `drizzle-kit generate` to create the SQL migration and `drizzle-kit migrate` to apply it. That's a production-ready Drizzle setup on a managed PostgreSQL, backed up and off the public internet.

Read the full guide: https://www.kloudbean.com/blog/connect-drizzle-to-postgres/

#Kloudbean #CloudHosting #DevOps #WebDev #PostgreSQL
```

## X thread
```
1/5  connect drizzle to postgres

Quick thread 🧵
```
```
2/5  Pick a driver (node-postgres, the `pg` Pool, or postgres.js), read your `DATABASE_URL` from an environment variable, and wrap the client in `drizzle()`.
```
```
3/5  Define your tables in a TypeScript schema, then run `drizzle-kit generate` to create the SQL migration and `drizzle-kit migrate` to apply it.
```
```
4/5  That's a production-ready Drizzle setup on a managed PostgreSQL, backed up and off the public internet.
```
```
5/5  Full walkthrough:
https://www.kloudbean.com/blog/connect-drizzle-to-postgres/
#Kloudbean #CloudHosting #DevOps #WebDev #PostgreSQL
```

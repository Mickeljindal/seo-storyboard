# Social posts: Fail2ban and Shorewall: The Two Tools Quietly Guarding Your Server

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/fail2ban-and-shorewall-guide/

## X / Twitter
```
> Shorewall is a host firewall: it configures the Linux netfilter/iptables layer to block everything by default and allow only the ports your services…

https://www.kloudbean.com/blog/fail2ban-and-shorewall-guide/
#Kloudbean #DevSecOps #CloudSecurity
```

## LinkedIn
```
> Shorewall is a host firewall: it configures the Linux netfilter/iptables layer to block everything by default and allow only the ports your services…

Fail2ban is a brute-force bouncer: it watches logs like the SSH auth log, and when an IP racks up failed logins, it bans that IP temporarily by adding a firewall rule. Together they cut the constant automated attacks that hit every public server. They work at the host and network layer, which is different from a WAF (application layer) and from DDoS protection (volumetric, at the edge).

Read the full guide: https://www.kloudbean.com/blog/fail2ban-and-shorewall-guide/

#Kloudbean #DevSecOps #CloudSecurity
```

## X thread
```
1/6  Fail2ban and Shorewall: The Two Tools Quietly Guarding Your Server

Quick thread 🧵
```
```
2/6  > Shorewall is a host firewall: it configures the Linux netfilter/iptables layer to block everything by default and allow only the ports your services actually need, so most of the internet's probing hits a closed door.
```
```
3/6  Fail2ban is a brute-force bouncer: it watches logs like the SSH auth log, and when an IP racks up failed logins, it bans that IP temporarily by adding a firewall rule.
```
```
4/6  Together they cut the constant automated attacks that hit every public server.
```
```
5/6  They work at the host and network layer, which is different from a WAF (application layer) and from DDoS protection (volumetric, at the edge).
```
```
6/6  Full walkthrough:
https://www.kloudbean.com/blog/fail2ban-and-shorewall-guide/
#Kloudbean #DevSecOps #CloudSecurity
```

# Social posts: fix eaddrinuse port already in use node

> Auto-generated from this article's title, meta description, and short-version box. No new claims. Edit freely before posting.
> Article: https://www.kloudbean.com/blog/fix-eaddrinuse-port-already-in-use-node/

## X / Twitter
```
Find what's holding the port and stop it.

https://www.kloudbean.com/blog/fix-eaddrinuse-port-already-in-use-node/
#Kloudbean #CloudHosting #DevOps #WebDev
```

## LinkedIn
```
Find what's holding the port and stop it.

On macOS or Linux: `lsof -i :3000` to see the process, then `kill -9 <PID>`. Or one shot with `npx kill-port 3000`. If it keeps coming back, the cause is usually a previous instance that never exited, a dev watcher starting twice, or two processes told to use the same port.

Read the full guide: https://www.kloudbean.com/blog/fix-eaddrinuse-port-already-in-use-node/

#Kloudbean #CloudHosting #DevOps #WebDev
```

## X thread
```
1/6  fix eaddrinuse port already in use node

Quick thread 🧵
```
```
2/6  Find what's holding the port and stop it.
```
```
3/6  On macOS or Linux: `lsof -i :3000` to see the process, then `kill -9 <PID>`.
```
```
4/6  Or one shot with `npx kill-port 3000`.
```
```
5/6  If it keeps coming back, the cause is usually a previous instance that never exited, a dev watcher starting twice, or two processes told to use the same port.
```
```
6/6  Full walkthrough:
https://www.kloudbean.com/blog/fix-eaddrinuse-port-already-in-use-node/
#Kloudbean #CloudHosting #DevOps #WebDev
```

{
  "version": "v1",
  "hooks": [
    {
      "name": "Generate social posts for new blogs",
      "trigger": "Stop",
      "description": "When an agent turn finishes, auto-generate social.md + social.json (X/Twitter, LinkedIn, thread) for any content-studio article that does not have them yet. Idempotent and fast: only touches articles missing social copy.",
      "action": {
        "type": "command",
        "command": "node social-studio/blog-social.mjs --missing",
        "timeout": 120
      }
    }
  ]
}
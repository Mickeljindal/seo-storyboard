{
  "name": "kloudbean-video-studio",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Local HTML->ffmpeg explainer-video generator for Kloudbean (30+ ICP-targeted scripts + animated storyboards + mp4 export).",
  "scripts": {
    "build": "node build.mjs",
    "export": "node export.mjs",
    "render-all": "node render-all.mjs",
    "setup": "npm install && npx playwright install chromium"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}

{
  "name": "kloudbean-social-studio",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Local generator for 100+ Kloudbean social posts (branded HTML card -> PNG) each with ready-to-paste caption text, across the 6 ICPs.",
  "scripts": {
    "build": "node build.mjs",
    "render": "node render.mjs",
    "setup": "npm install && npx playwright install chromium"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
